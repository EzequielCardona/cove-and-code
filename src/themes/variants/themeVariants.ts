/**
 * Defines the theme variants. For the moment it's only dark or white
 * but its built to hold other theme variations like cold or warm
 */
export enum themeVariants {
  dark = 'dark',
  light = 'light',
}
